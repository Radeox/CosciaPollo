services:
  bot:
    container_name: CosciaPollo
    build: .
    command: python bot.py
    env_file: .env
    volumes:
      - .:/app/
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
